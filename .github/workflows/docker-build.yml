name: Docker Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
      
    - name: Build Backend Docker image
      uses: docker/build-push-action@v4
      with:
        context: ./Backend
        file: ./Backend/Dockerfile
        push: false
        tags: options-tracker-backend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    - name: Build Frontend Docker image
      uses: docker/build-push-action@v4
      with:
        context: ./Frontend
        file: ./Frontend/Dockerfile
        push: false
        tags: options-tracker-frontend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # Uncomment to push to Docker Hub or GitHub Container Registry
    # - name: Login to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKERHUB_USERNAME }}
    #     password: ${{ secrets.DOCKERHUB_TOKEN }}
    #
    # - name: Push to Docker Hub
    #   run: |
    #     docker push your-username/options-tracker-backend:latest
    #     docker push your-username/options-tracker-frontend:latest
